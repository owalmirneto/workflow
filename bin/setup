#!/usr/bin/env bash

set -e;

source $(dirname "$0")/functions;

echo "# Adiciona scripts do interage/git-flow ao PATH" >> ~/.bashrc;
echo "export PATH="'$PATH'":$PWD/bin" >> ~/.bashrc;

success_message The path setted;
