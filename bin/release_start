#!/bin/bash

set -e

bin/ci

RELEASE_VERSION=${1}
COMMIT_HASH=${2}

echo -e "\e[1;36m$ git flow release start $RELEASE_VERSION $COMMIT_HASH \e[0m"
git flow release start $RELEASE_VERSION $COMMIT_HASH
