#!/bin/bash

set -e

RELEASE_VERSION=${1}
COMMIT_HASH=${2}

echo -e "\e[1;36m$ release_start $RELEASE_VERSION $COMMIT_HASH \e[0m"
release_start $RELEASE_VERSION $COMMIT_HASH

echo -e "\e[1;36m$ release_finish $RELEASE_VERSION \e[0m"
release_finish $RELEASE_VERSION
