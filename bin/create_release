#!/bin/bash

set -e

bin/ci

RELEASE_VERSION=${1}
COMMIT_HASH=${2}

git flow release start $RELEASE_VERSION $COMMIT_HASH
bumpversion
git flow release finish -Fp $RELEASE_VERSION $COMMIT_HASH
git checkout master && git merge develop && git push origin master
