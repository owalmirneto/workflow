#!/bin/bash

set -e;

source config;

TRELLO_SLUG_CARD=${1};
BRANCH_NAME=feature/card-$TRELLO_SLUG_CARD;

execute git checkout -b $BRANCH_NAME --track origin/develop;

execute git push -u origin $BRANCH_NAME;

success_message Feature started successfully.;
